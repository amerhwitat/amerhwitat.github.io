# Chimera II Public Web UI

This deployment surface publishes the Chimera II Aurora Three.js/WebGL desktop and consumes the shared Web UI contracts from `amerhwitat/test`. It is a sandboxed presentation surface, not a native Linux or Windows execution environment.

Use versioned manifests for desktop profiles, unified help, terminal, IDE, and CPU/ISA visualization features. Native backends require explicit authorization outside browser code.
