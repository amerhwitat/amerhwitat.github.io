# Web Implementations

The site now has a dedicated `web/javascript/` entry point and PHP service boundary. Project demos should be isolated into independently deployable pages, use responsive standards-based HTML/CSS/JavaScript and avoid privileged operations.