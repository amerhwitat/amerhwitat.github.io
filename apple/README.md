# Apple companion application

The public research site remains web-first. This directory defines an optional native SwiftUI/Xcode companion shell for iOS/iPadOS and macOS that can consume public portfolio metadata and documentation without duplicating private content.

IPA generation requires macOS/Xcode.
